<?php declare(strict_types = 1);

namespace PHPSTORM_META;

override(
	\Orisai\ObjectMapper\Rules\RuleManager::getRule(0),
	type(0)
);

override(
	\Orisai\ObjectMapper\Context\TypeContext::getRule(0),
	type(0)
);

override(
	\Orisai\ObjectMapper\Creation\ObjectCreator::createInstance(0),
	type(0)
);

override(
	\Orisai\ObjectMapper\Processing\Processor::process(1),
	type(1)
);
